'use strict';

var store= {
     days: ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday']
     hours: []

var locations: ['Downtown', 'Ballard', ]
}

function getRandomInt(max) {
    return Math.floor(Math.random()) * Math.floor(max);
}